import { ComingSoon } from "@/components/ComingSoon";

export const metadata = { title: "Receive stock" };

export default function ReceivePage() {
  return <ComingSoon title="Receive stock" phase={3} />;
}
